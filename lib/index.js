const {
  encodeOutcome,
  getChannelId,
  hashAppPart,
  hashOutcome,
  hashState,
} = require('../native/index.node')

module.exports = {
  encodeOutcome,
  getChannelId,
  hashAppPart,
  hashOutcome,
  hashState,
}
